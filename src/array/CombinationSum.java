package array;

import java.util.List;

/**
 * Given a set of candidate numbers (C) and a target number (T), find all unique combinations in C where the candidate
 * numbers sums to T. <br>
 * The <b>same</b> repeated number may be chosen from C unlimited number of times. <br>
 * <b>Note:</b> All numbers (including target) will be positive integers. Elements in a combination (a1, a2, … , ak) must be in
 * non-descending order. (ie, a1 ≤ a2 ≤ … ≤ ak). The solution set must not contain duplicate combinations.<br>
 * For example, given candidate set 2,3,6,7 and target 7, A solution set is: [7] [2, 2, 3]
 * 
 * @author Bohan Zheng
 *
 */
public class CombinationSum {
	public List<List<Integer>> combinationSum(int[] candidates, int target) {
		return null;
	}
}
